# 密钥与凭据
.env
# 改 .env 前留的备份。写 `.env` 一条不够 —— gitignore 不做前缀匹配，
# `.env.bak.x` 照样是未跟踪文件，而它和 .env 一样装着明文凭证，绝不能进版本历史。
# （animal-bot 早就有这条规则，这边一直漏着，2026-09-03 补上。）
.env.bak.*

# 运行时状态与临时产物（可随时重建）
state/
logs/
pick.json
content.json
img_urls.jsonl
data/queue.add.tsv
data/queue.add.tsv.ok

# fetch-material.py 的 41MB 维基索引缓存（脚本注释里已声明 gitignored，但此前漏写）
.cache/

# openclaw agent 工作区引导文件与运行产物
AGENTS.md
BOOTSTRAP.md
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md
openclaw-workspace-state.json
.openclaw/

# Python 缓存
__pycache__/

# zhconv 装在这里（fetch-material.py 的可选依赖，见其文件头）
vendor/
